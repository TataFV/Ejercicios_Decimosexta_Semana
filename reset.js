/*
var dni = document.getElementById("nif")
if (dni == "")
    button.addEventListenet("click", (evt) => {
        function restaurarCampos(dni, nombre, appelidos);
    });
    */
function restaurarCampos(dni, nombre, appelidos) {
    /*
    for (let i = 0; i < .length; i++)
        var field = children[i].value == ""
    do {
        document.removeChild('nif')
    } while (field = false);
*/
}

